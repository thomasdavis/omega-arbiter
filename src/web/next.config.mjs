/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Allow the app to be built in the src/web directory
  distDir: '../../.next',
};

export default nextConfig;
